# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Test output
test-results/
playwright-report/
coverage/

# Cache
.vite/
*.tsbuildinfo
.eslintcache

# Editor files
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# tmp
*.tmp
*.orig
.codex

# Tool directories
.playwright-mcp/
.serena/
.spec-workflow/
.zcf/plan/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment
.env.local
.env.*.local

# CLAUDE.md 用硬链接和 AGENTS.md 建立链接，不用版本管理
CLAUDE.md
.claude/skills/

src/types/auto-imports.d.ts
src/types/auto-components.d.ts

/stats.html


# TIPS：如果某些文件已经加入了版本管理，现在重新加入 .gitignore 是不生效的，需要执行下面的操作
# `git rm -r --cached .` 然后提交 commit 即可。

# git rm -r --cached file1 file2  ## 针对某些文件
# git rm -r --cached dir1 dir2  ## 针对某些文件夹
# git rm -r --cached .  ## 针对所有文件