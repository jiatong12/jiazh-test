import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:house-outline',
  title: '工作台',
  isHide: false,
  isFull: false,
  isAffix: true,
  isKeepAlive: true,
  order: -100,
})
