import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:id-card-outline',
  title: '令牌管理',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 0.8,
  priv: 'Platform.Token',
})
