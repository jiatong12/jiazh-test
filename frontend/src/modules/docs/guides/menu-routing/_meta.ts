import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:sitemap-outline',
  title: '菜单与路由约定',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 7,
})
