import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:stethoscope',
  title: '问题排查索引',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 10,
})
