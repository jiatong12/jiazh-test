import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:dock-window',
  title: '弹层生命周期与回调',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 9,
})
