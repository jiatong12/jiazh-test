import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:shield-account-outline',
  title: '字典与权限约定',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 5,
})
