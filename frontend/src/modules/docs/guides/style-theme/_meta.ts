import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:palette-outline',
  title: '样式与主题约定',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 3,
})
