import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:image-outline',
  title: 'BaseImage',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 19,
})
