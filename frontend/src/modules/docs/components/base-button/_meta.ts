import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:gesture-tap-button',
  title: 'BaseButton',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 11,
})
