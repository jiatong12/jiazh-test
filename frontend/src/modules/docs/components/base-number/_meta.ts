import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:numeric',
  title: 'BaseNumber / BaseDecimal',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 15,
})
