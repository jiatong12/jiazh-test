import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:tag-outline',
  title: 'BaseTag',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 14,
})
