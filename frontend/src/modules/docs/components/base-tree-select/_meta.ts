import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:file-tree-outline',
  title: 'BaseTreeSelect',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 16,
})
