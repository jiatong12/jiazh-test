import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:clipboard-text-outline',
  title: 'BaseForm',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 2,
})
