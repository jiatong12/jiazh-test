import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:card-outline',
  title: 'BaseCard',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 5,
})
