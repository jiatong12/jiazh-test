import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:form-dropdown',
  title: 'BaseSelect',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 12,
})
