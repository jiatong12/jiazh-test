import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:tune-variant',
  title: '选项类组件',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 18,
})
