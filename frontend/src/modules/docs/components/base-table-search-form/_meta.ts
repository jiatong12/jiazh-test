import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:form-select',
  title: 'BaseTableSearchForm',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 4,
})
