import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:table-large',
  title: 'BaseTable',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 3,
})
