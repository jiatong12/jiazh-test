import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:upload-outline',
  title: 'BaseUpload',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 20,
})
