import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:application-brackets-outline',
  title: 'BaseDialog',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 8,
})
