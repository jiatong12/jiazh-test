import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:dock-right',
  title: 'BaseDrawer',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 9,
})
