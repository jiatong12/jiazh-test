import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:cloud-sync-outline',
  title: 'useRequest',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 3,
})
