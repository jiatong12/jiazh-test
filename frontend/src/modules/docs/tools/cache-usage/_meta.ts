import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:database-lock-outline',
  title: '缓存使用约定',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 4,
})
