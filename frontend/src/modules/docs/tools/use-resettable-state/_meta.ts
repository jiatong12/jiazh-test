import { buildMeta } from '@/utils'

export default buildMeta({
  icon: 'i-mdi:restore',
  title: 'useResettableState',
  isHide: false,
  isFull: false,
  isAffix: false,
  isKeepAlive: true,
  order: 6,
})
